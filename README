
Use the INSTALL executable to install the required python environment and partis.

After installing the conda environment needs to be loaded every time before use, like this:
source activate AMP




